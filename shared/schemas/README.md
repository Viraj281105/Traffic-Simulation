# Shared — Schemas

JSON Schema definitions (Draft 2020-12) for all data contracts. These are the canonical, machine-validatable source of truth.

Files: `snapshot.schema.json`, `config.schema.json`, `metrics.schema.json`, `messages.schema.json`, `errors.schema.json`
