# Scripts

Development automation, CI/CD helpers, and validation utilities.

## Planned Scripts

- `setup.sh` — One-command project setup (install all dependencies)
- `validate-schemas.sh` — Validate JSON files against shared schemas
- `run-all-tests.sh` — Run backend and frontend test suites
