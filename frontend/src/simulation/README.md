# Frontend — Simulation

Simulation visualization on HTML5 Canvas: renderers (vehicles, roads, intersection, signals, roundabout), frame interpolation, playback controls.

**Never put here:** Simulation logic, vehicle physics, controller decisions. The frontend renders — it never simulates.
