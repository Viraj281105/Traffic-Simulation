# Frontend — Components

Generic, reusable, domain-agnostic UI building blocks: buttons, cards, modals, form inputs, spinners.

**Never put here:** Page-level components, business logic, API calls, simulation rendering.
