# Backend Source — Config Module

Configuration loading, validation against JSON Schema, default value application, and application settings.

**Never put here:** Business logic, simulation algorithms, hard-coded scenario parameters.
