# Backend Source — Metrics Module

Real-time metric collection, computation, and aggregation. Each metric is an independent, self-contained computation unit in `definitions/`.

**Never put here:** Simulation control logic, vehicle physics, API serialization.
