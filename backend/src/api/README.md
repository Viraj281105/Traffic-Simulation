# Backend Source — API Module

HTTP and WebSocket endpoint definitions. Thin adapter layer that delegates all business logic to service modules.

**Never put here:** Simulation logic, metric computation, vehicle physics.
