# Backend Source — Events Module

Publish-subscribe event bus for decoupled inter-module communication.

**Never put here:** Business logic implementations (handlers should delegate to appropriate modules).
