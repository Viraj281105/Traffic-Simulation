# Backend Source — Intersection Module

Intersection geometric layout, approach arms, conflict zones, and signal phase definitions.

**Never put here:** Controller decision logic (use `controllers/`), vehicle movement, metric computation.
