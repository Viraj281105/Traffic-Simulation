# Backend Source — Utils Module

Generic utility functions used across multiple backend modules: math helpers, validators, logging, ID generation.

**Never put here:** Domain-specific logic. If a utility is specific to one module, it belongs in that module.
