# Backend Source — Controllers Module

Intersection control strategy implementations. Each controller implements a common abstract interface.

Current controllers: Fixed-Time Signal, Roundabout.

**Never put here:** Vehicle physics, metric calculations, API endpoints.
