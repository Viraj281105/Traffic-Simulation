# Backend Source — Snapshot Module

Snapshot assembly, JSON serialization, WebSocket emission, and history buffering. Strictly read-only — reads simulation state but never modifies it.

**Never put here:** Simulation logic, metric formulas, controller decisions.
