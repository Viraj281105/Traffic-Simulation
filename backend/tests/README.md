# Backend Tests

All backend tests organized by type.

- `unit/` — Unit tests per module (mirrors `src/` structure)
- `integration/` — Cross-module integration tests
- `fixtures/` — Test data (sample configs, expected snapshots)
